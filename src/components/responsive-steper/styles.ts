import styled from 'styled-components'

export const StepWrapper = styled.div`
  display: flex;
  width: 80%;
  background-color: #fff;
  padding: 1rem;
  align-items: center;
  border-radius: 20px;
  h3 {
    color: var(--primary);
  }
`
