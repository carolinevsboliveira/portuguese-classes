import styled from 'styled-components'

export const StepWrapper = styled.div`
  display: flex;
  width: 80%;
  background-color: #fff;
  margin: 0.5rem;
  padding: 0.5rem;
  align-items: center;
  border-radius: 20px;
  h3 {
    color: var(--primary);
  }
`
